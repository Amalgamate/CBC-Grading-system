╔══════════════════════════════════════════════════════════════════════════════╗
║                    STREAM SEEDING - QUICK COMMAND REFERENCE                  ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│ 🎯 GOAL: Add streams A, B, C, D to all schools                              │
└──────────────────────────────────────────────────────────────────────────────┘

╔══════════════════════════════════════════════════════════════════════════════╗
║                         ⚡ FASTEST METHOD (30 seconds)                       ║
╚══════════════════════════════════════════════════════════════════════════════╝

cd server
npm run seed:streams

✅ Creates streams A-D for all active schools
✅ Skips if already exists
✅ Shows pretty output

╔══════════════════════════════════════════════════════════════════════════════╗
║                         📋 ALL METHODS                                       ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────┬────────────────────────────────────────┐
│ Method                              │ Command                                │
├─────────────────────────────────────┼────────────────────────────────────────┤
│ Streams Only (Recommended)          │ npm run seed:streams                   │
│ Full Seed (Users + Schools + etc)   │ npm run seed                           │
│ SQL Direct                          │ psql -f prisma/seed-streams.sql        │
│ Via Prisma Studio                   │ npx prisma studio → Import             │
└─────────────────────────────────────┴────────────────────────────────────────┘

╔══════════════════════════════════════════════════════════════════════════════╗
║                         🔍 VERIFICATION COMMANDS                             ║
╚══════════════════════════════════════════════════════════════════════════════╝

# Check streams in Prisma Studio
npx prisma studio

# Check via SQL
psql -d your_db -c "SELECT s.name AS school, sc.name AS stream FROM stream_configs sc JOIN schools s ON s.id = sc.\"schoolId\" ORDER BY s.name, sc.name;"

# Count streams per school
psql -d your_db -c "SELECT s.name, COUNT(sc.id) AS streams FROM schools s LEFT JOIN stream_configs sc ON sc.\"schoolId\" = s.id GROUP BY s.name;"

╔══════════════════════════════════════════════════════════════════════════════╗
║                         📂 FILES LOCATION                                    ║
╚══════════════════════════════════════════════════════════════════════════════╝

TypeScript:  server/prisma/seed-streams.ts
SQL Script:  server/prisma/seed-streams.sql
Main Seed:   server/prisma/seed.ts (updated)
Guide:       STREAM_SEEDING_GUIDE.md

╔══════════════════════════════════════════════════════════════════════════════╗
║                         ✅ EXPECTED RESULT                                   ║
╚══════════════════════════════════════════════════════════════════════════════╝

After running, each school should have:
┌──────────────────────┬────────┬────────┐
│ Stream Name          │ Active │ School │
├──────────────────────┼────────┼────────┤
│ A                    │ 🟢 Yes │ All    │
│ B                    │ 🟢 Yes │ All    │
│ C                    │ 🟢 Yes │ All    │
│ D                    │ 🟢 Yes │ All    │
└──────────────────────┴────────┴────────┘

╔══════════════════════════════════════════════════════════════════════════════╗
║                         🎨 CUSTOMIZATION                                     ║
╚══════════════════════════════════════════════════════════════════════════════╝

Want different names? Edit seed-streams.ts line 30:

  const streamNames = ['A', 'B', 'C', 'D'];

Change to:
  const streamNames = ['North', 'South', 'East', 'West'];
  const streamNames = ['Red', 'Blue', 'Green', 'Yellow'];
  const streamNames = ['Alpha', 'Beta', 'Gamma', 'Delta'];

╔══════════════════════════════════════════════════════════════════════════════╗
║                         ⚠️ TROUBLESHOOTING                                   ║
╚══════════════════════════════════════════════════════════════════════════════╝

Problem: "No active schools found"
Solution: npm run seed  (creates default school)

Problem: "Stream already exists"
Solution: Normal! Script skips existing streams

Problem: "Cannot find module @prisma/client"
Solution: npx prisma generate && npm run seed:streams

Problem: "Database connection refused"
Solution: Start PostgreSQL service

╔══════════════════════════════════════════════════════════════════════════════╗
║                         📊 QUICK SQL SNIPPETS                                ║
╚══════════════════════════════════════════════════════════════════════════════╝

# View all streams
SELECT s.name AS school, sc.name AS stream, sc.active
FROM stream_configs sc
JOIN schools s ON s.id = sc."schoolId"
ORDER BY s.name, sc.name;

# Delete all streams (CAUTION!)
DELETE FROM stream_configs;

# Delete streams for one school
DELETE FROM stream_configs WHERE "schoolId" = 'school-id-here';

# Count streams
SELECT COUNT(*) FROM stream_configs;

# Deactivate stream A
UPDATE stream_configs SET active = false WHERE name = 'A';

╔══════════════════════════════════════════════════════════════════════════════╗
║                         🚀 WORKFLOW                                          ║
╚══════════════════════════════════════════════════════════════════════════════╝

1. cd server
2. npm run seed:streams
3. Check output - should see "✅ Created stream A/B/C/D"
4. Verify in UI: Settings → Academic Settings → Streams
5. Done! ✨

╔══════════════════════════════════════════════════════════════════════════════╗
║                         💡 PRO TIPS                                          ║
╚══════════════════════════════════════════════════════════════════════════════╝

✅ Run seed:streams after creating each new school
✅ Script is idempotent (safe to run multiple times)
✅ Streams are case-sensitive (A ≠ a)
✅ Keep stream names consistent for reporting
✅ Backup database before bulk operations

═══════════════════════════════════════════════════════════════════════════════
Quick Start: cd server && npm run seed:streams
═══════════════════════════════════════════════════════════════════════════════
