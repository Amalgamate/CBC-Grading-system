/**
 * Head Teacher Dashboard - Department oversight view
 */
import React from 'react';
import Dashboard from '../Dashboard'; // Use existing dashboard for now

const HeadTeacherDashboard = (props) => {
  return <Dashboard {...props} />;
};

export default HeadTeacherDashboard;
